name: YTDownloader
options:
  bundleIdPrefix: com.antigravity
targets:
  YTDownloader:
    type: application
    platform: iOS
    bundleId: com.antigravity.YTDownloader
    deploymentTarget: "15.0"
    sources: 
      - path: YTDownloader
    settings:
      INFOPLIST_FILE: YTDownloader/Info.plist
  YTShareExtension:
    type: app-extension
    platform: iOS
    bundleId: com.antigravity.YTDownloader.YTShareExtension
    deploymentTarget: "15.0"
    sources:
      - path: YTShareExtension
    settings:
      INFOPLIST_FILE: YTShareExtension/Info.plist
